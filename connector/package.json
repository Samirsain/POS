{
  "name": "pos-thermal-connector",
  "version": "1.0.0",
  "private": true,
  "description": "Runs on the office PC the printer is paired to. Separate from the web app: serialport is a native module and has no business in a serverless deploy.",
  "main": "connector.js",
  "scripts": {
    "start": "node connector.js",
    "ports": "node connector.js --ports",
    "test-print": "node connector.js --test"
  },
  "dependencies": {
    "serialport": "^13.0.0"
  }
}
